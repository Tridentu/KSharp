using Sys;
using Sys.Application;

namespace KSharp.Tests {
    public class MultiFileApp : ConsoleApplication {
        public static void Main(string[] args) {
            DataModel model = new DataModel();
            model.Increment();
            Console.WriteLine($"Count: {model.Count}");
        }
    }
}
