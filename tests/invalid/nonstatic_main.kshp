using Sys;
using Sys.Application;

namespace KSharp.Tests {
    public class BadEntry : ConsoleApplication {
        // Main is not static — should generate a warning
        public void Main(string[] args) {
            Console.WriteLine("This should warn!");
        }
    }
}
